# LeadScout — instructions for Claude Code

Read `LeadScout-build-prompt.txt` (full spec) before changing UI. `design/leadscout-design-v4.html` is the current visual language — logo, per-tool colours, Plus Jakarta Sans headings, rounded containers and badges, two-panel login — and overrides the older files where they disagree. The earlier files still hold the layout and copy of individual screens: `leadscout.html` (rail screens), `leadscout-home-v2.html` (Home), `verify-v2.html` (Verify).

Tool colours only ever come from `src/lib/tool-colour.ts` — Tailwind cannot build a class name at runtime, so `bg-tool-${x}` renders nothing. A colour says which tool; `ok`/`warn`/`bad` say how a fact stands. Never mix the two.

Every screen is checked at desktop and 390px: `SCREEN_BASE=… npx tsx --env-file=.env.local scripts/design-shots.ts` signs in as a real user, shoots both widths and fails on sideways scroll.

## The rule that overrides everything
Never fabricate. Every contact, email, phone, date or "verified" flag must come from a fetched page (stored with URL + time) or be shown as `pattern` / `unknown`. `src/lib/ai/claude.ts#appearsIn` is the check — keep using it. No Math.random() in data paths. Nothing is emailed except through `/api/outreach`, by a recruiter, to an address attached to a contact/company.

Two corollaries learned the hard way:
- **A model asked for JSON will answer under different keys.** `askJson` takes the required keys from the schema itself and retries once with the zod complaint fed back. Never add a prompt that describes its shape in prose only.
- **A claim about our own people needs evidence behind it.** Bullets are audited against the profile (`checkBullets`), draft emails against the pool (`checkDraft`). Both name the offending phrase rather than silently dropping it.

## Database changes go through a numbered migration — never the dashboard
Every structural change to the database — enabling RLS, a policy, a column, an index, a function, a grant — is a new file in `supabase/migrations/` with the next free number, committed, then applied by hand. **Never make it in the Supabase dashboard**, not even "just to try": a dashboard change leaves no trail in migration history, so nobody reading the migrations can know it exists. That is exactly how RLS came to be switched on for `articles`, `lead_articles` and `lead_people` with no policy — no migration enables it — and every signed-in user read none of those rows for weeks (no "+N sources", no "Also reported", no people from Industry Contacts, every lead's age unknown) while the service-role jobs saw everything and nothing looked broken. If the dashboard is the only way to try something, write the migration that reproduces it before anything relies on it. `scripts/rls-sweep.ts` is in the release gate and fails it when a signed-in user reads fewer rows of any table than they should. The same sweep (`src/lib/rls-sweep.ts`) runs **nightly inside the recheck cron** (`/api/jobs/recheck`, 00:00 — both cron slots are taken) — the only thing that notices a dashboard change made between commits (until 2026-09-14 it had **never** stored a nightly result: a manual run showed its first request getting an HTML gateway page and its save getting "Gateway Timeout", with no retry on either — both now retry, and it runs first in the recheck job); `/api/jobs/rls-sweep` runs it on demand behind the cron secret. Tables without `workspace_id` are counted through the parent their policy uses, so another workspace's rows never make a correct table look hidden. Tables only the service role may read (`jobs`, `job_ticks`) are listed in `SERVICE_ONLY` and pass only while a signed-in user reads none of their rows. Creating the sweep's throwaway account retries up to three times with the same address, and an "already registered" answer is looked up and used — a single "Gateway Timeout" there at 14:30 on 2026-09-14 turned Home red for everyone and failed the gate, and a fresh address on retry would strand an account a timed-out create had in fact made. The sweep's throwaway account enters the real workspace as a **recruiter** and is removed with `removeProbe` (one retry); a removal that still fails comes back as `cleanupError` from `/api/jobs/rls-sweep` and fails smoke — on 2026-09-14 a single failed delete during a Supabase outage left it inside the real workspace as a senior, and smoke passed anyway. **Home shows the last result to everyone** on a "Data access check" pill (`data-pulse="rls"`): red naming the hidden tables or saying the sweep could not run, amber when no result is kept (0026 not applied) or no run has reported in 36 hours, green with the time it passed. Results live in `health_checks` (0026), verdicts only, never row counts; 0026's `rls_tables_without_policy()` also names a table with RLS on and no policy even when it has no rows, which counting cannot judge. **A policy can exist and still be wrong, and neither check sees it:** `users` had `for all using (id = auth.uid() or workspace_id = my_workspace())` with no `with check`, so a user could rewrite their own role and workspace and any teammate's row. `scripts/users-policy-probe.ts` tries those writes as a user in the release gate. Any policy with an `or` in it and no `with check` gets the same test before it ships.

## Before every commit, and after every deploy
Standing instruction from the user:

```
bash scripts/release-gate.sh <port>          # all of the below in order; runs every step, exits 1 if any failed
npm run typecheck && npm run build
npx tsx --env-file=.env.local scripts/rls-sweep.ts        # every table as a signed-in user: RLS on with no policy fails here
npx tsx --env-file=.env.local scripts/users-policy-probe.ts  # throwaway accounts: changing your own role or workspace, or a teammate's row, fails here
npx next start -p <port>                     # serve the build you are about to commit
npx tsx --env-file=.env.local scripts/pdf-check.ts
npx tsx --env-file=.env.local scripts/pdf-name-audit.ts   # every stored client PDF, every version
npx tsx --env-file=.env.local scripts/verify-e2e.ts       http://localhost:<port>
npx tsx --env-file=.env.local scripts/lead-drawer-e2e.ts  http://localhost:<port>
npx tsx --env-file=.env.local scripts/smoke.ts            http://localhost:<port>
```

After deploying, run `scripts/smoke.ts https://leadscout-rfbt.vercel.app` and report pass/fail per flow. Against a deployed base smoke first waits for `/api/health` to report the commit under test and exits 2 if it never matches — a run started seconds after a push tested the previous build and reported four failures against correct code. If anything regresses, fix it before moving on — never leave production broken to continue a feature. Kill the old `next start` first (`taskkill //PID <pid> //F`): a stale server serves old chunks and produces false failures.

**Migrations are applied by hand by the user, so a deploy can land before its schema.** Never name a new column without guarding it — `src/lib/schema-features.ts#hasColumn` asks once per process. A missing column fails the *whole* query, which has taken Leads, the drawer and Verify down.

**A migration that adds a unique index must dedupe inside the migration.** 0014 failed on live data holding the same source_url twice. Adding the index is not the job; making the data satisfy it is. Keep the oldest row so `first_seen_at` still means what it says, and re-point references before deleting.

**A migration that adds a column referencing another table breaks every existing embed between those two tables.** 0013 added `candidates.eu_passport_document_id` and `candidates.uk_right_to_work_document_id`; with `documents.candidate_id` that made three relationships, PostgREST could no longer resolve `candidates(documents(...))`, and it failed the *whole* query — Candidates, the public `/v/<slug>` page and two of Today's six queries all went quiet on the day it was applied. Name the foreign key in every such embed: `documents!candidate_id(...)`.

**No name goes in a storage path, even an internal one.** Every upload site built its key from the file the recruiter dropped, so object storage held `…/cv/1789031300347-Bertescu_Dumitrel_CV_Final_Readable.pdf`. That is never served to a client, but a name in a path turns up in logs, backups, bucket listings and signed URLs, and none of those are places anyone thinks to look. `src/lib/storage-path.ts#documentPath` is the only way to build one: workspace, type, candidate id, a twelve-character digest of the original name, and the extension. Fourteen existing objects were moved by `scripts/rename-document-paths.ts`.

**`scripts/pdf-name-audit.ts` runs whenever a new anonymized CV is generated, not once.** It reads the *rendered* client PDFs — drawn text, the document information dictionary, the storage path, the download filename — because nothing else in the codebase reads the bytes that actually reach a client. Three separate defects in the reading each produced a false "clean" before it was trustworthy (the newline before `endstream` is optional; @react-pdf writes text as hex runs inside TJ arrays; the Info dictionary holds indirect references to UTF-16BE objects), so it now refuses to call a file clean when it extracts under 40 characters or reads no properties. It audits every version, not just the latest: an older PDF is still an object somebody may hold a URL to.

**The candidate tables hold sensitive personal data — take extra care with every change.** `candidates`, `documents`, `sends` and `candidate_placements` (0035) hold names, phone numbers, emails, dates of birth, passports and right-to-work facts, work history and where a named person works. Any change to them — a column, a policy, a query that reads them for a new screen, a script that writes them — gets a row-level policy with a `with check`, `scripts/rls-sweep.ts` as a signed-in user before it is called done, and a note here saying why. `candidates.data_retention_until` (0035) exists so a keep-until date can be set and queried; the deletion policy itself is not decided yet. Test data lives only in workspaces marked is_test and is removed by `removeProbe`.

**A screen must never report an absence it did not check.** Candidates read `{ data }` and ignored `{ error }`, so a query that never ran rendered as "No candidates yet" and looked like a true empty pool for weeks. Read the error, and say a query failed when it failed.

**`findOrCreateCompany` is the only way to create a company.** Four jobs each matched on their own `ilike name`, which is how "Equinor" and "Equinor ASA" became two rows and the unique index would not build. `src/lib/company-identity.ts` holds the one rule; it is deliberately cautious, because a wrong merge moves another company's provenance onto the survivor with no way back.

## State of the codebase
- **Migrations 0001–0032 applied** (0030–0032 on 2026-09-14). 0029 is the crawl schedule (pg_cron, pg_net, `job_ticks`; the `leadscout_cron_secret` Vault secret first) — confirmed 2026-09-14: ticks at 14:20:03, 14:25:01, 14:30:04 UTC. 0030 is Industry Contacts events on `people`: the WindEurope Annual Event 2026 list was imported the same day — 11,031 rows read in one response, 1,884 already on file gained the event (846 with a changed title kept in `title_history`), 9,117 new, 26 repeated inside the list, 4 unusable; people 12,582 → 21,699. 0031 is `industries` and `industry_evidence` on leads and companies — backfilled 154 of 154 leads and 150 of 150 companies. 0032 is `users.industry_follow` and `industry_limit`, with the trigger that enforces them — `scripts/industry-follow-probe.ts` passed all 16 checks against production the same day. 0028 (2026-09-14) takes every write on `users` away from signed-in users. 0001's policy let any account change its own role, move itself into any workspace whose id it knew, and rename, re-role, change the onboarding day of, or delete a teammate's row — all six proven on the live database with throwaway accounts, while the real database held one account, a senior, so nothing had been exploited. `scripts/users-policy-probe.ts` tries those writes and is in the release gate. Item 18 grants no write on `users` either: a person's industries are saved only by `/api/me/industries` and `/api/team/industries` through the service role, and 0032's trigger checks every write. 0027 is `jobs`: RLS on explicitly, with a service-role-only policy. 0026 is the RLS sweep's `health_checks` and `rls_tables_without_policy()`. Its catalogue check's first run (2026-09-13 21:11 UTC) named `jobs` — the worker queue, RLS switched on outside the migrations, no policy, no rows, so counting as a user could never see it — and Home's pill went red for everyone, as designed. Nothing a user reads touches `jobs` (worker/index.ts uses the service role), so no screen was broken; 0027 records that intent. 0024 is `articles.award_date` and `award_date_basis`, filled from each notice's stored text (matched the text on 110 of 110 award leads: 46 award decision, 63 contract concluded, 1 neither); 0025 is the read policies on `articles`, `lead_articles` and `lead_people`. 0022 is the contacts policy (a contact on a company is visible to that company's workspace — checked 2026-09-13 as a signed-in user reading the table directly: 3 company-level and 31 lead contacts, the same as the service role); 0023 is item 14's schema.
- **Item 14 — a news story becomes a visible lead only with a real trigger and a quoted company representative worth writing to.** Rules, no extra model call: `src/lib/radar-filter.ts` (trigger, early-stage, developer/auction, timeline) and `src/lib/quoted-contacts.ts` (role exclusion, employer from the article text, rank). Owner's decisions 2026-09-13: FEED, LOI pending FID, concept studies and engineering-only awards, and a developer winning a site are not triggers; work starting over 12 months out is low confidence, over 18 rejected; a quoted person from another company counts only if that company is a target account (active lead status or pursued hiring — McDermott, today). Stage 1 adds `company_role` in the same call (€0.00767 per article measured, €0.00770 before). Rules read **prose only** — stored pages open with site navigation that names the company ("CompanyAbout UsNews…McDermott Difference") and the first version read that as the story. Every judgement, rejection included, goes to `radar_verdicts` (0023) and `radar_runs.rejected`; nothing is dropped, no status is changed. `scripts/radar-filter-report.ts` re-judges stored leads (2026-09-13: 13 of 24 surfaced news leads would be hidden; Nadara rejected, DWT keeps Olsen over EWE's Bolduan); `--write` stores those verdicts, only when both examples pass — 29 written 2026-09-13 (24 surfaced, 5 already hidden by status), no lead touched. Backfilled the same day: 7 email patterns over 6 companies; 25 news sources flagged ok, 4 left for the browser-backed recheck, 110 award notices not checked. Email shapes per company (`src/lib/email-pattern.ts`, `company_email_patterns`): own domain or its parent only, glued text recovered only when a printed name reproduces it, one-word locals never corroborate. Source flags (`src/lib/source-quality.ts`, `leads.source_flag`): a paywall, sign-in or landing page is stored, flagged and not extracted; the nightly recheck writes flags, never status, and skips award notices (their pages 429 a burst). Checks: `radar-filter-check.ts`, `email-pattern-check.ts`, `source-quality-check.ts`.
- **Item 17 — lead age is computed on read and is never a status.** `src/lib/lead-age.ts`: a news lead is ageing at 45 days and a stale signal at 90, from its article's `published_at`; an award lead at 180 and 365, from the award decision, else the contract's conclusion, else the notice's publication (0024 stores the first two as `articles.award_date`); a Hiring now row is as old as its newest advert and ageing at 60, from the posting date, else first seen. Ageing and stale rows dim and sort lower on Leads, Hiring now and Today and are never hidden; no date reads "age unknown" and sorts with fresh. "Stale signal" is not the recruiter status `stale`. **Re-advertised** means the same company and role on a different day inside 180 days — several adverts on one day are openings, and the old Hiring now wording called wet pro's three and AIBEL's two same-day adverts reposts "not filled"; a role on three or more days goes to the top of Hiring now and into Today. Impact 2026-09-13 (`scripts/lead-age-report.ts`, award dates read from the notice text): news on screen 24 — 6 fresh, 1 ageing (Bechtel, 62 days), 17 unknown; awards 110 — 105 fresh, 4 ageing, 1 stale (BEIRENS, 503 days); postings 38 — 30 fresh, 8 ageing, 28 aged from first seen; company rows (agencies hidden) 14 — 13 fresh, 1 ageing; roles raised 0 (Kymar re-advertised once). With 0024 applied, 109 of 110 award leads read older than they did from their notice's publication (up to 498 days, BEIRENS) and 5 changed state; all 110 had read fresh before. **The Leads table loads 50 award leads by fit before age sorts them**, so an award lead outside those 50 is not on screen at all — on 2026-09-13 BEIRENS (stale, 503 days), BR Ingenieurbau and Tiefbau Gotha (ageing, 251 days). The crawl closes a company's open postings that are no longer on its board, but only when the board changed and at least one trade title was kept (`seenUrls` in `src/lib/jobs/job-posts-batch.ts`); an unchanged, empty or unreadable board closes nothing — on 2026-09-14 it closed Vestas' three 10 September adverts as three new ones arrived. Re-adverts appear only as new URLs arrive. Checks: `lead-age-check.ts`; smoke seeds a 100-day article, a 70-day advert and a role advertised on three days. **"Latest activity" sort** (`?sort=latest` on Leads and on Hiring now; `latestActivityCompare`): Fresh, Ageing, Stale, then Age unknown — undated goes last in this mode only — and newest first within each, each row judged against its own source's thresholds. Leads reads every open lead in this mode, not the top 50 of each kind by fit, and sorts news and awards together; Hiring now keeps re-advertised rows first. Fit and Pressure stay the defaults, and the sort survives the source chips, the filters, the agencies toggle and opening a row.
- **A table anchored on a company needs a policy that says so.** 0016 fixed it for job_posts; contacts kept 0001's `lead_id in (leads of my workspace)` until 0022, so every organisation-page contact (lead_id null) was invisible to signed-in users — Karstensens' drawer showed a switchboard and no name while René Hansen sat in the table, and AIBEL's HR head was missing too. The job that writes them runs as service role and saw them fine. `api/hiring` reads them with the service role scoped to a company already loaded under the user's RLS; `scripts/drawer-contacts-probe.ts <base>` signs in as a real-workspace user and checks every stored company-level contact is on screen with its title. Any new company-level table: write the policy, then check it as a user, never as the job. **The same again for `articles`, `lead_articles` and `lead_people` (found 2026-09-13):** RLS is on for all three with no policy and no migration enabling it, so a signed-in user read 0 of 605 articles, 0 of 141 article links and 0 of 146 Industry Contacts links — no "+N sources" on Leads since item 12, no "Also reported" in the drawer, no "N from Industry Contacts", every lead's age unknown. `src/lib/lead-articles.ts` reads them with the service role for leads already loaded under the user's RLS; 0025 adds the read policies (applied 2026-09-13 — a user then reads 141 of 141 article links, 146 of 146 Industry Contacts links, and 134 of 605 articles, exactly the articles behind the workspace's leads; the other 471 back no lead and stay service-only). **`scripts/rls-sweep.ts` checks every table at once** — service role against a signed-in real-workspace user — and exits 1 on any table the user reads less of; run it after any migration and before trusting a new screen. It cannot judge a table with no rows (on 2026-09-13: jobs, campaigns, campaign_candidates, cert_unknown, outreach).
- **Spend is metered and capped for Radar's extraction too.** `askJson` takes an `onUsage` meter; Radar logs every attempt of `extractLead` and `extractJobPost` to cost_log and checks `Budget.canAfford` **before fetching and storing** each article (a stored article counts as seen and is never read again). A spent cap hard-stops the batch and the chain. Measured 2026-09-13: €0.0077 per extraction, €0.0075 per job-post read. Unmetered before that: 9 Sep really €9.96 (cost_log said €8.64), 10 Sep €3.55 (said €1.05). Since item 16 (2026-09-15) every other model call is metered as well — see the item 16 note below. Home's "Spent today" turns amber at 80% of the cap and red at the cap (`data-pulse="spend"`).
- **Dates come off the page, never out of guesswork.** `src/lib/page-dates.ts`: an article's date only from Article/NewsArticle/BlogPosting `datePublished`, `article:published_time`, `og:published_time`, `itemprop=datePublished` or a `<time>` inside `<article>`; a posting's only from `JobPosting.datePosted`. WebPage dates and sidebar `<time>` are the site's, not the story's (parkwind.eu says 2020 for a 2024 story). A dateline in the text needs a publication cue ("Published", "Press release", "News") and 0021. A crawl no longer blanks a posted_at it cannot re-read (`src/lib/posting-date.ts`). Backfill 2026-09-13: 227 of 482 news articles, 10 of 38 postings; 239 pages state no date at all.
- **Company names keep their letters in any script.** `canonCompany` kept a-z0-9 only, so "Ørsted" read as "rsted" and a Bulgarian TED winner as nothing. Now \p{L}\p{N}, legal forms stripped only as whole words; nothing is transliterated. 59 of 5,837 keys changed, no new matches. A name with nothing comparable left is stored exactly as written and matched only on the identical string. `scripts/company-identity-check.ts`, `scripts/page-dates-check.ts`.
  0011 source tiers + `radar_runs` · 0012 employer-type override · 0013 right to work + `workspaces.candidate_countries` · 0014 job boards · 0015 campaigns · 0016 job_posts RLS + `workspace_id` · 0017 employer-type evidence · 0018 trade cards + glossary + onboarding review · 0019 `cert_library` + `cert_unknown` + attach trail · 0020 posting contacts, company contact page, hiring row state, `outreach.company_id`, `is_test` on eight tables.
- **Radar**: 600 sources tiered by Haiku — priority 44 / standard 189 / off 366, each with a stored reason. Priority daily, everything on Sundays, one cron. `repair-sources` finds a newsroom again when a URL rots and follows a rebrand (Wood → woodgroup.com). 29 won-work leads.
- **Contract awards (item 12)**: TED only, `src/lib/tender/`. The search API (`POST api.ted.europa.eu/v3/notices/search`) is anonymous but throttles without saying so — 25 back-to-back requests drew 429s after 10 — so `ted.ts` spaces requests 1.1 s and backs off on 429. `cpv.ts` holds 42 trade codes, each checked against the eForms SDK codelist by `scripts/cpv-check.ts`; award types are can-standard/can-social/can-tran (can-modif, veat and can-desg are not wins). **A notice counts only when its procedure's main classification is on the list**: matching any code made leads of tilers, landscapers and a lift company on school builds. Each notice is stored as an article and each winner becomes a won_work lead pointing at it (`#winner-N` when there are several). No model call. Rides Radar's cron inline on the first batch (60 s budget); `/api/jobs/tenders` and `scripts/run-tenders.ts` for manual, backfill and dry runs. Backfill 2026-08-14 → 09-13: 345 notices, 105 awards on a trade main classification, 112 winners, 109 leads, 2 linked as the same contract (Gabčíkovo, published twice), 0 errors, 4 API requests, 72 s, €0. A winner written only in Cyrillic ("ЕКОВАТ - БЪЛГАРИЯ ЕООД") is rejected as unusable, because `canonCompany` keeps only a-z0-9.
- **Same contract, two sources**: `src/lib/same-contract.ts`. News ↔ award: same company_id AND the award's contracting authority named in the story, within 365 days. Award ↔ award: same company, same authority, same title. More than one candidate lead → nothing merged. The second source is linked through lead_articles, never a second lead. The earlier record becomes canonical **only when both dates are real** — news articles carry no date (`articles.published_at` is empty on every Radar row), and first-read is not publication: Aker Solutions' KN Energies release says 19 March, its TED notice 23 March, Radar read the release on 10 September. `scripts/same-contract-probe.ts` proves the rule on is_test rows.
- **Source tag**: `src/lib/lead-source.ts` reads News / Tender award off the lead's source URL — no column, nothing to migrate. The Leads table reads news and award leads as **two lists of 50**, with All / News / Tender awards chips (`?source=`). One list of 50 by fit let the backfill take 45 of the 50 rows, because an award naming nobody scores the same 80 as a story with a quoted decision-maker. A quoted decision-maker wins a tie. Today's new-leads item still ranks both kinds together by fit alone.
- **Doffin** waits for the official Public API key (subscription required; the owner is signing up). Do not build on `api.doffin.no/webclient` — it is the site's own undocumented API. **Udbud.dk** is out: 100 of its 100 newest award notices carried TED numbers and its detail endpoint has no structured winner. Radar's existing page crawls of udbud.dk, doffin.no, ted.europa.eu and simap.ted.europa.eu (all enabled, priority, type tender) read nothing — each is a JavaScript app — and last_crawled_at is null on all four.
- **Hiring now**: careers/ATS discovery **complete** — 348 boards found, 193 none, 44 unreachable. 16 ATS vendors; Workday needs a POST. Crawl: fingerprint → Haiku titles (which also map trades from the source language) → Sonnet bodies, €2/day cap. 38 open postings across 17 companies, one row per company, sorted by pressure with the reason on hover, filter chips for country/trade/employer/pressure, agencies hidden by default. Each row opens a drawer: postings grouped, employer type with override, Confirm, the four tools reading from the adverts, "Ready to attach" per trade, and a drafted approach. Job boards are the **secondary** source: poster and employer recorded separately, an unnamed employer stays unnamed, and an advert repeating a company's own page is marked a duplicate.
- **An employer type says where it came from, and a job id is never a title (2026-09-14).** The careers-page classifier (`api/jobs/classify-employers`) refused any reply whose evidence ran past 300 characters: 57 companies stored "could not be read: too_big" as evidence, 51 stayed unknown (four on Hiring now — StS gruppen, Kemp & Lauritzen, Baker Hughes, Yabimo Norge AS) and 6 kept the guess `findOrCreateCompany` makes from the name with nothing to say so — EnBW Offshore Wind Norway, a wind-farm owner, read as an EPC contractor because its name says "offshore". Evidence is now trimmed, not refused; the name guess is written with `employer_type_source: 'name'` and its reason, and a failed read labels any surviving type the same way. `scripts/fix-employer-and-board.ts` requeued the 57 and labelled 519 name guesses (6 more types left unlabelled: their origin cannot be shown). DOF's "EPC contractor" is not this bug: it is a careers-page reading with quoted evidence, and "DOF" gives the name detector nothing. DOF's advert was titled "1924855": its board is `dof.workable.com`, which detection did not know, so the crawl fell back to rendered links and used the last path segment as the title. `detectAts` now knows a company's own Workable host, the browser fallback reads any board a rendered page links to, and `cleanTitle` refuses a title with no word in it or a requisition code (`needsPageTitle` sends it to the posting page instead). Crawled again: 92 adverts read, one trade role — outside Europe — and the numeric row closed. `scripts/board-title-check.ts`.
- **A failed auth check is not a sign-out (2026-09-14).** The smoke check on the Hiring now drawer's advert age failed three times that day, last as "no drawer on the page"; with the page's location recorded it was on `/login` ("Welcome back"), HTTP 200, mid-session, and the very next page load was signed in again. `getUser()` calls the auth server on every request, and a call that failed in transit returned no user, which the middleware and `currentUser()` both read as signed out. Both now go through `steadyUser` (`src/lib/supabase/steady-user.ts`): a missing or rejected session is still an immediate "nobody", but a retryable fetch error, status 0, 429 or 5xx is asked up to three times. A middleware redirect to /login carries `x-signin-reason`, and the smoke check prints each redirect hop with it, so a repeat says whether the auth server failed three times, the session was really gone, or the page itself redirected. **It repeated on 2026-09-15 as the page's own redirect** (307, no middleware header), again straight after smoke's RLS sweep call and signed in again on the next load. Not expiry: the access token lives 3,600 s (`scripts/session-length-probe.ts`, throwaway account). Not the sweep's account handling: it creates, updates and deletes only its own address. The screens' redirects now go through `requireUser()` (`src/lib/supabase/server.ts`), which sends `/login?why=page: <auth answer>`, and smoke prints the URL it landed on — the next repeat names the answer the screen got. **It did (2026-09-15, gate on item 21): `/login?why=page: AuthRetryableFetchError 0`** — the screen's getUser never reached the auth server on any of its three quick retries, while the middleware's check on the same request had passed; straight after the RLS sweep's minute of traffic the local server's kept-alive connections to the auth server can all be stale. `steadyUser` now tries six times over about two seconds and returns `failed` when it could not ask; a failed check never redirects — the middleware lets the request through and `requireUser` throws "The sign-in service could not be reached … You are still signed in — reload the page." A missing or rejected session still goes to /login. **It still reached recruiters as a bare "Application error"** (found in item 23's gate, 2026-09-15, on mobile Candidates): a production build strips a server error's message, and there was no error boundary anywhere. `src/app/error.tsx` — at the root segment, so it also catches the /app and rail layouts, where requireUser runs (a segment's own error.tsx does not catch its own layout) — and `src/app/global-error.tsx` (the root layout) show "Something went wrong — reload the page" with a Reload button, a link Home and the error's digest as a reference (`data-error-boundary`, `data-error-reference`). design-shots and smoke now count that page as a failed screen: they matched only "Application error", so every broken screen would otherwise pass. `/app/error-check` fails on purpose for an account in a test workspace and is a 404 for everyone else; `scripts/error-boundary-probe.ts <base>` reads the page at 1500px and 390px, presses Reload, and checks a real-workspace account gets the 404.
- **A kept employer type keeps its origin (2026-09-14).** Before re-reading the 57 too_big companies, `classify-employers` was found to label any type it kept over an "unknown" verdict as `careers_page`, so a name guess the page did not settle (EnBW Offshore Wind Norway as an EPC contractor) would have come back looking like a careers-page reading. `verdictPatch` (`src/lib/employer-verdict.ts`) keeps a name guess labelled `name`, with the page's non-answer as evidence; `scripts/employer-verdict-check.ts`. The route also takes `ids=` (a named set, read before the rest of the queue) and clamps `cap` to `DAILY_BUDGET_EUR`, so no caller can spend past the daily budget. The re-read itself is `scripts/reclassify-driver.ts`, measured by `scripts/reclassify-snapshot.ts` against `.cache/reclassify-before.json`.
- **The crawl names its workspace, and the €2.00 cap is system-wide (2026-09-15).** A sign-up gets its own empty workspace (`handle_new_user`, 0001), and the crawl files everything under the workspace that owns its sources, so a new customer sees nothing on day one and nothing is ever crawled for them (the shared-crawl design is a separate, held conversation). The first real second workspace ("Leadscout", the owner's own test sign-up, 2026-09-14 22:38 UTC) exposed eleven jobs that picked "the workspace" with an unordered `workspaces.select('id').limit(1)` — directories, companies, seed-domains, classify-sources, classify-employers, job-boards, resolve-domains, classify, registries, the job-posts batch, and radar-batch's fallback — so any of them could file companies, people and spend under the empty workspace. They now call `crawlWorkspace(db)` (`src/lib/crawl-workspace.ts`), which names it through the TED sources row exactly as tender ingest does, and refuses to run without one. The cap was counted per workspace, so a job that picked the empty workspace saw €0 spent and got a second €2.00; `spentTodayEur(db)` and `Budget.open(db, cap)` now read every workspace's cost_log rows, paged (one unpaged read stops at 1,000 rows; 10 September logged 944), throw rather than report €0 when the spend cannot be read, and clamp any `cap` to `DAILY_BUDGET_EUR` — the classify job's default of €5 is now €2. resolve-domains' all-time search cap and Home's "spent today" read the same system-wide figure. `scripts/workspace-scope-check.ts` is in the release gate: no workspace-id `limit(1)` pick anywhere in src, the crawl workspace equals the sole owner of the sources, the cap figure equals an exact paged all-workspace sum (of spend that is not test traffic, since item 16), and `Budget.open(db, 99)` is capped at 2.
- **Item 19 — compound signals boost, never replace (2026-09-15).** `src/lib/compound-signals.ts` (pure) and `compound-signals-load.ts` (as the signed-in user). Three signal types per company, each counted only when dated inside 60 days: a TED award (award date, else notice publication, else Radar's first reading, named as such), a news lead (article date, else first reading), and an open posting (posted, else first seen). **A re-advertised role is the posting signal, stronger — never a type of its own** (owner's decision 2026-09-15, recorded under item 19 in the queue): the first cut counted a role item 17 raised as a fourth type, so any raised company reached two types from its own careers board alone; a boost now needs a posting together with an award or a story. A raised role still dates the posting signal and is named in the reason. Two or more types multiply the stored fit of every open won-work lead of that company by 1 + 0.1 per extra type, capped at 100 and still at 25 outside Europe (a boost the cap swallows says so); pressure goes one step up. Computed on read, never stored: the row shows "boosted ×1.1: tender award + open Hiring now posting" and "boosted, was 60", the drawer (`data-compound-why`) each signal with its date and basis, and a failed read says so instead of showing no boost. Measured 2026-09-15 (`scripts/compound-signals-report.ts`): 120 companies with a signal inside 60 days, 119 with one type, **one with two** — Baker Hughes, a news story (Plaquemines LNG, US) and an open posting, both 14 September: its lead's fit stays 25 (the outside-Europe cap), its Hiring now pressure low → medium. AIBEL has a story and a posting but the story is outside the window; Kymar Group re-advertised a role once. 18 of 149 won-work signals are dated only by Radar's first reading. `scripts/compound-signals-check.ts` (in the gate); smoke seeds "Smoke Compound AS" (award + posting today) and reads the boost on Leads (60 → 66), on Hiring now (low → medium, `data-pressure` "boosted, was low"), in both drawers, at 390px, and "(boosted)" on Today — and checks that "Smoke Readvert AS", which only re-advertised its own role, is not boosted. Hiring now's `groupByCompany(postings, compounds)` sorts and filters on the lifted pressure; Today ranks new leads by boosted fit and counts a company lifted to high. `scripts/compound-screen-probe.ts <base> [--hiring]` signs in as a recruiter of the real workspace and reads every boosted company's rows and drawers — on production 2026-09-15 Baker Hughes read "boosted ×1.1: news mention + open Hiring now posting" beside fit 25, "boost held by the cap", at 1500px and 390px.
- **Item 21 — Won work gets the company-site contacts Hiring now had (2026-09-15).** One engine: `discoverAtCompany` (`src/lib/company-contact-discovery.ts`) is item 13's per-company chain moved out of `api/jobs/hiring-contacts` unchanged — contact/careers page (switchboard, general or HR address, only filling blanks), organisation page, named people on the company's own sites — and the route now runs it for Hiring now companies and every company behind an open won-work lead. People a news story quoted are looked for by name in the same crawl; `addToQuotedContact` gives them only a missing email or phone with the page as its source, never replacing, renaming or re-titling them. `contacts_checked_at` is the cache: a company is read once in 30 days however many leads or postings stand on it; a company with no site on file is skipped unstamped. The route was open to any POST until then — anyone could spend the day's budget — and now needs the cron secret (`scripts/run-hiring-contacts.ts [--scope wonwork]` sends it and reports companies read more than once in a run). Paid search stays in `scripts/attendee-contacts.ts --search` (quoted people too, `--quoted-only`), under the cap; `resolve-domains` checked only its own all-time €100 search limit and now also stops at the daily cap. Census before (`scripts/wonwork-contacts-census.ts --save`): 149 open won-work leads over 129 companies — 117 tender awards (111 companies, none with a contact), 32 news leads with a quoted person (18 companies, no quoted person with an email or phone), 0 news leads without one (item 14 shows none); 9 companies behind more than one lead; **103 of the 129 have no website on file** (89 of 111 winners), so discovery cannot reach them — owner's decision: measure the 26 with a site first, the 103 get prepared searches, website finding decided on that yield (its past runs logged €0.098 a company). On screen: the lead query now loads the quoted contact's email, sources and quote (the embed never did, so the drawer could show neither), the Decision-maker cell falls back to a company-site person or the switchboard, and the lead drawer has "From the company's own site" (people, switchboard, general email, each with its page) or prepared searches that say they are searches and whether the company has no website, has not been read, or printed nothing. Smoke's quoted "Smoke Person" seed had no source and broke contacts' 0001 check, so it had never existed — the insert error was not read. **Measured on production (2026-09-15, `scripts/wonwork-contacts-compare.ts` against the saved census):** discovery read 25 companies for 35 leads in 86 s, a second pass straight after read none, and no company was read twice (Worley's 6 leads, Aker Solutions' 5 and WALKE + ACKER's 2 each one read; `scripts/wonwork-contacts-cache-check.ts`); none of the 103 without a site was stamped. **10 of 149 leads gained a contact they did not have** — tender awards 9 of 117 (8 companies: 3 switchboard, 2 general email, 4 both), news with a quoted person 1 of 32 (VATTENFALL, switchboard), news without one 0 of 0. No quoted person gained an address: 21 of the 36 lacking one work at companies with no site, the other 12 are printed nowhere their site's free pages or a search reach. Organisation pages: 2 of 25 home pages linked one (3 read), naming nobody with a hiring title. Paid search against the full set (`attendee-contacts.ts --won-work --search --write`): 24 searches, **€0.4208, nothing found** — 21% of the day's cap; not a candidate for any automated crawl. Free discovery cost about €0.03. Screen: `scripts/wonwork-contacts-screen-probe.ts` read Fläminger, WALKE + ACKER, Kattner and Heinen's rows and drawers on production at 1500px and 390px as a real-workspace recruiter. **It also showed two switchboards cut short:** `phoneOn` stopped at an en dash or a slash inside a printed number, storing Kattner Stahlbau as "+49 (0)3435" (the page prints "+49 (0)3435 – 666 2-0") and Daldrup & Söhne as "+49 (0) 25 93" ("… / 95 93 - 0") — 2 of the 16 switchboards on file, a number a recruiter would dial and reach nobody. The reader now continues through those separators and refuses anything under 9 digits; `scripts/repair-short-switchboards.ts --write` re-read both source pages and stored the whole numbers (a value is replaced only by the same digits, whole; anything else is cleared); `scripts/phone-on-check.ts` is in the gate.
- **A tender winner's address is in the notice's XML, and searching with it did not help (2026-09-15).** The award ingest stores only the place of performance (`place-of-performance-city-lot`) — the contract's site, often the buyer's — and it is empty on ALLEZ ENERGIES' lead. The winner's own PostalAddress is in TED's published XML for the notice (ted.europa.eu/en/notice/<number>/xml), linked from the award section (`efac:TenderingParty` → `efac:Tenderer` → `ORG-…`): ALLEZ ENERGIES is "Ld La Nautiere, 16260 Chasseneuil-Sur-Bonnieure, FRA" while the same notice lists the buyer in Angoulême and a review court in Poitiers. `src/lib/tender/winner-address.ts` reads it by that link (`scripts/winner-address-check.ts` on a committed copy of the notice, in the gate). The website lookup moved out of resolve-domains unchanged into `src/lib/domain-lookup.ts` and takes an optional address. **Owner's hypothesis tested on 6 domainless tender winners, each looked up both ways** (`scripts/domain-lookup-sample.ts`, report only; 2 of the 8 notices sampled printed no winner address): name + country €0.0209 a company, a domain for 6, the notice's town or postcode printed on the site for 3; name + address €0.0247 a company (+18%, a longer prompt and more second hops), a domain for 5, verified 3 → 2. The one difference: ANDRES VIZOSO — name + country found vizoso.net, whose home page prints BURELA, the notice's town; with the address the stricter rule answered no domain. Five of six gave the same domain either way. On this sample the address belongs in checking a found site (free: the site prints the postcode or town, or it does not), not in the search. The measured €0.021 a lookup also corrects the €0.098 a company taken from resolve-domains' 9 September cost_log, which included Sonnet experiments: the 103 without a site are about €2.20 to look up, a little over one day's cap. The route still sends name, country and sector only.
- **Websites for Won work's companies with none (owner's go-ahead 2026-09-15; `scripts/resolve-wonwork-domains.ts --write [--limit N]`, resumable, stops at the cap).** Name + country lookup, then the free check: the site's home page or a contact / imprint page prints the notice's postcode or town; stored as `source` 'web search · address printed on the site' / '… not printed on the site' / '… site did not load for the address check' / '… no address in the notice'; a miss is marked `careers_status` 'no_domain_found' and never paid for again. Batch 1 (08:36 UTC, 35 of 104): **26 websites, 9 none, €0.8067 — €0.0230 a company**; address printed on the site 11, not printed 2 (ALLEZ ENERGIES → allez.fr, COLAS FRANCE → colas.com), site did not load 4, notice gave no address 9. Contact discovery then read the 26 for 27 leads (none twice): **10 leads gained a contact — 7 tender awards, 3 news leads (Bechtel, Damen, DWT) — every one a switchboard or general email; named people with an email or phone: 0, and 0 of 151 leads have one** (`scripts/wonwork-contacts-compare.ts` now reports the split). Three things to know: (1) the lookup is not deterministic — ANDRES VIZOSO, found as vizoso.net in the sample (its home page prints the notice's town), came back "no website" here and is now marked not found; (2) several matches are a group's site, not the local winner's (colas.com for COLAS FRANCE, bechtel.com, damen.com), so a switchboard there is the group's front door; (3) a contact read off a site whose address check said "not printed" (ALLEZ ENERGIES' general email) shows the same as one from a verified site. The other 69 run after the cap resets.
- **The other 69 are paused by the owner until three accuracy fixes (2026-09-15).** (1) **A miss is final only on the second lookup** — `domain_lookups` (0033) counts them, `careers_status 'no_domain_found'` is written on the second miss, and `resolve-wonwork-domains.ts` does one lookup per company per run (`--retries-only` for the due retries); `resolve-domains` had the same one-miss rule and its queue filtered on `careers_checked_at`, which a miss never set, so it paid again for every company it had missed — both fixed. 0033 gives every company marked "no website" after one lookup its retry (15: batch 1's 9 and 6 WindEurope Industry Contacts companies from 9 September). The script's run without `--write` looked companies up and threw the answers away — a lookup is paid either way — so it now only lists what it would do. (2) **A group's site is told from the winner's own** — `src/lib/site-scope.ts`: a branch word in the winner's name ("ETABLISSEMENT", "agence", "Niederlassung") on the brand's site; a country in the name ("FRANCE", "Belgium") on the brand's generic or foreign domain (colas.com for COLAS FRANCE, aellia.com for Aellia Belgium NV), but not on its own country's (ccecc.ro for "… ROMANIA SRL"); or a domain already on file for a differently named company. Stored as `domain_scope` / `domain_scope_reason`, and worked out on read where nothing is stored. (3) **Confirmed and unconfirmed look different** — `src/lib/site-trust.ts` → the lead drawer's "From the company's own site" block opens with "Confirmed: <domain> prints the award notice's address (…)" or "Confirmed: the award notice itself gives <domain>" in the ok colour, or the group warning and "Not confirmed: … does not print the award notice's address (…) — check it is the right company before calling" in the warn colour on a dashed border; the row adds "· unconfirmed", "· group site". **0033 (`domain_provenance`) must be applied by hand**: it adds `domain_source`, `domain_source_url`, `domain_checked_address`, `domain_address_check`, `domain_scope`, `domain_scope_reason`, `domain_lookups`, `domain_looked_up_at`; moves batch 1's check out of `companies.source`, which a search wrote over, and puts back where each company came from (its TED notice, or its Radar story for Bechtel, Damen and DWT); the old plain 'web search' rows from 9 September keep that source, because nothing records where they came from. After it, `scripts/backfill-domain-provenance.ts --write` fills the checked address and the scope for the websites found by search. Until it is applied everything reads batch 1's source text, and the resolve script refuses to write. Checks: `site-scope-check.ts`, `site-trust-check.ts`, `winner-address-check.ts` in the gate; smoke seeds a confirmed site (Smoke Tender Winner AS) and an unconfirmed group site (Smoke Groupe France SAS on smoke-groupe.com) and reads both drawers at desktop and the warnings at 390px, in the phone window it already has open. **0033 applied 2026-09-15:** batch 1's 26 checks moved into the columns (11 printed, 2 not printed, 4 site did not load, 9 no address), provenance restored (23 'ted award notice', 3 'radar article'), 15 companies given their retry (9 behind Won work leads; E. Edal Support, TS TECH, MS Wincon, Vento Maritime, ITW Densit and Storm Managment are WindEurope Industry Contacts companies with no lead, so a retry adds nothing to Won work). `backfill-domain-provenance.ts --write` over all 101 websites found by search: 96 own site, 5 group site (GAC Denmark on gac.com with GAC Norway, RWE OFFSHORE BELGIUM on benelux.rwe.com, COLAS France ETABLISSEMENT COTE BASQUE on colas.fr, Aellia Belgium NV on aellia.com, COLAS FRANCE on colas.com), 17 notice addresses. Its first report called 13 group sites: a domain shared with another row counted even when the other row was the same company under another name — Winergy / "Winergy / Flender", Siemens Gamesa / "… Renewable Energy", "NIDEC SSB Wind Systems" / "Nidec SSB Windsystems", CREADIS / "DIS/CREADIS", "DTU WIND" / "DTU Wind & Energy Systems", "RWE Offshore" / "RWE Offshore Wind" — six duplicate company rows, a separate identity problem. A shared domain now counts only when neither name contains the other. The production screen check (`scripts/site-trust-screen-probe.ts`) then showed ALLEZ ENERGIES right at both widths, and COLAS FRANCE and Aellia with no warning at all: their sites gave no contacts, so the drawer showed prepared searches, which read "Nothing printed on its own site" — calling colas.com COLAS FRANCE's own. The trust lines now show above the searches too, and the sentence names the domain.
- **"Industry Contacts" is the name on screen for people from event lists (owner's rename, 2026-09-15).** Labels only: the Leads help row, the row's "N from Industry Contacts", the people-read error, the lead drawer's "People at this company from Industry Contacts", Hiring now's contact badge (the `where` value stays `'attendee list'` in code), the "read from …" line, the API error messages and the draft's reason. Stored values keep their old words — `people.source` ("WindEurope Copenhagen attendee list"), `companies.source` ('WindEurope attendee list') — and every table, column, function and file name is unchanged (`people`, `lead_people`, `attendeesAt`, `src/lib/attendee-match.ts`, `scripts/attendee-contacts.ts`); `src/lib/industry-contacts-label.ts#industryContactsLabel` relabels a stored source wherever one is shown.
- **Spain (item 22, 2026-09-15).** Three gaps, measured before building: (1) **353 Spanish companies, all from Industry Contacts (the WindEurope list), none with a domain** — resolve-domains had no ES in its country list; with ES added its other filters (one of its seven sectors, and an ops-relevant Industry Contact at the company) leave **38**, 194 are in those sectors, 159 are 'irrelevant'. Owner's decision: the 38 first (`/api/jobs/resolve-domains?country=ES&chain=0`), then decide. Navantia is four rows — NAVANTIA, NAVANTIA SEANERGIES and SEANERGIES are in the 38, NAVANTIA S.A. S.M.E. is not (no ops-relevant contact) — and none has a lead or posting, so contact discovery never reached them: `api/jobs/hiring-contacts` now takes `ids` (`run-hiring-contacts.ts --ids a,b,c`) to read named companies. (2) **No Spanish job board, and no EURES source at all** — 41 job-board sources, 9 enabled, none Spanish; item 4's named national boards (Jobindex, Finn.no, CV-Library) are in the table and disabled, Werk.nl and Reed are absent. Tecnoempleo is IT-only ("Empleo IT") — rejected. InfoJobs answers plain crawling with a bot challenge (Distil / captcha) but has an official, documented API (developer.infojobs.net) that needs a registered client id and secret (`api.infojobs.net` → 401 "Client credentials not valid" without) — owner chose the API and registers the app. trabajos.com serves plain HTML but was not confirmed free of aggregation. EURES's search is a JavaScript app with no public API page. (3) **Cádiz is covered once, by TED, and it is not RFBT's project**: notice 590638-2026, turbine-washing maintenance on F-80 frigates at the Arsenal de Cádiz, won by INTEGRACION TECNOLOGICA EMPRESARIAL, S.L. — already a Won work lead. "Navantia" appears once, in an offshore-energy.biz page's sidebar ("Navantia UK delivers…"). No Spanish news or naval source exists, so a Cádiz EPC / corrosion project is not being missed by a crawl — nothing covers it.
- **Item 16 — every model call is metered (2026-09-15).** Before it only the crawl logged: the recruiter tools, Verify's document read, the anonymiser and CV transcription spent without a row, and hiring-contacts logged a flat €0.01 per read — 212 such rows, €2.12, on 8–15 September, none of them tokens. **Recruiter tools** (`src/lib/ai/tools.ts#RECRUITER_TOOLS`, 13 names) log every attempt, a failed first one included, to cost_log with `kind` = the tool, the model, input+output tokens, EUR at `RATES`, the workspace and "by <user id>": `meterRecruiter(me, fn)` wraps each of the seven routes (anonymize, anonymize/enrich, candidate/questions, hiring, lead, verify, verify/intake) and `asTool(name, fn)` names each call in documents.ts (AsyncLocalStorage; the innermost name wins, so the bullet audit is its own tool). `askJson` without a meter and `createMessage` (a PDF or image; `transcribeCv` is now the one CV transcription) log under whatever is in scope, and a call with nothing in scope still logs — no workspace, kind 'unattributed' — so a wiring gap shows as a row, not as missing spend. **Recorded, never blocked by the cap** (owner's decision, re-issued 2026-09-15: do not re-ask); a real recruiter's calls count in the day's total, so a busy recruiter day stops the crawl earlier. A failed insert is printed and retried once (the first gate lost a €0.0026 PII review to one "fetch failed"). **A probe's spend is real**: a test workspace's rows say "test workspace", and `deleteTestWorkspace` detaches them (workspace_id null, row kept) because cost_log.workspace_id has no cascade and would block the delete. **Automated jobs**: hiring-contacts' advert and organisation-page reads pass `jobMeter` (real tokens, added to the Budget; the `canAfford(0.01)` check before each read stays as an estimate), and classify-sources and seed-domains — which logged tokens but never checked the cap — now open the Budget and stop. `scripts/model-meter-check.ts` in the gate reads the source (every recruiter route metered, every askJson named or metered, every raw `claude.messages.create` followed by logModelCall, no flat placeholder, every job route that calls the model opens the Budget) and exercises the meter on a fake database; each rule was shown to fail on a mutated file. **Measured on test traffic, per attempt** (the step 1 gate, €0.198, and production smoke, €0.075): candidate questions €0.021, screening questions for a job €0.015, CV parsing €0.014, job description €0.011, bullet audit €0.008, client summary €0.007, document read €0.007, bullets €0.006, PII review €0.003 — a CV through Verify with bullets, audit, summary and PII review about €0.044 before retries. Not yet called since metering: outreach draft, outreach audit, candidate scoring (score_pool scores up to 60 candidates, one call each), CV transcription. **Test traffic is logged and reported, never counted against the cap** (owner's decision, 2026-09-15). That day, before the decision, the release gates and production smoke runs were counted: €0.90 of recruiter-tool calls, 45% of the cap, took the day from €1.94 to €2.87 and stopped the automated jobs for real users for the rest of it. The cap protects production — Radar, website lookups, hiring-contacts. `cost.ts#isTestSpend` is the one rule: a row whose detail begins "test workspace" (a probe's own workspace, marked by the meter) or "test run (<script>)" (a gate script calling a tool outside a request — `LEADSCOUT_TEST_RUN`, set only in `release-gate.sh` for pdf-check), or whose workspace is marked is_test. `spentEur`, `spentTodayEur`, `Budget.open` and resolve-domains' search limit count only the rest; when the workspaces cannot be read, nothing is treated as test (the cap counts too much, never too little). A gate costs about €0.20 and a production smoke about €0.075 of test traffic. `scripts/cost-report.ts [--since] [--until] [--after] [--real]` prints per tool (who starts it, attempts, EUR, EUR per attempt, test share) and per day against the cap; rows from before item 16 are grouped by their detail's first words. Home's "Spent today" pill (`data-spend-total`, `data-spend-recruiter`, `data-spend-test`, `spentTodaySplit`) shows the total counted against the cap, the recruiter-tool part of it, and test runs beside it as "not counted", says so when spend cannot be read (it read €0.00), and at the cap says recruiter tools still run; `scripts/spend-screen-probe.ts <base>` reads it as a recruiter at 1500px and 390px against the database. **Step 2's first gate found a bug the metering did not cause:** Verify intake and the anonymiser read `next_reference_code`'s `.data` and ignored `.error`, so one failed call created candidate "MARIAN M" with no reference code, the screen showed none, and enrich answered 500 on `code.toLowerCase()`; verify-e2e's "with the reference code shown" passed, because it compared null with null. `src/lib/reference-code.ts#nextReferenceCode` reads the error, tries twice and refuses to create the candidate without a code; enrich answers 409 for a candidate with none (0 real candidates had one); verify-e2e now requires RFBT-<letter>-<number>.
- **Item 23 — which certificate registers can be checked automatically (investigated live 2026-09-15).** Six schemes had adapters from 2026-09-08/09; none had ever run on a real certificate (0 verifications on the six — the only certificates on file are 3 ISO 9606 and 4 FROSIO), and IRATA's drove a hosted browser through a reCAPTCHA, which the owner's rule rules out: a register behind a captcha or a login stays manual. **CSWIP (TWI) — public.** cswip.com/verification posts JSON `{certificateNo, dob: "yyyy/MM/dd"}` to `/api/CSWIPVerificationAPIController/VerifyCertificate`; the call carries no captcha token (the page loads reCAPTCHA's script, but this form has no widget and cswip.js sends none). The page's own success handler reads `candidateName`, `candidateNo`, `dateOfBirthStr`, `photograph` and `certifcates[]` (sic) with `role`, `level`, `certificateNumber`, `expiryDate` / `expiryDateStr` and `employerSponsored`, and says "This verification is valid on <date> only". Every error — a made-up number answered HTTP 500 with no body — renders the same "If you experience any problem … contact verification@twi.co.uk", so an error is not a "not found". Keyed on date of birth; no rate limit stated. **AMPP — public.** ampp.org's certification-search URL is now a 404; "Find A Certified Professional" leads to nace.useclarus.com/view/verify/, whose form posts JSON `{filter_join: "and", filter_expression: ["number startswith …", "last_name startswith …"], limit}` to `/data_api/view_credential_verification` — no sign-in, no captcha, `Cache-Control: no-store`, no rate-limit headers. A row carries name, location, contact phone and address (never stored) and, per credential in `agg_data`, `name`, `slug` (100 credentials, e.g. AMPP_BCI), `number`, `issued_on`, `expiration_date`, `program_category`; a made-up number answers `{"data": []}`. AMPP's page: results "reflect candidates who are considered current/active and have opted to display their credentials in the registry" — absence is not invalidity. **IRATA — manual.** techconnect.irata.org/verify/tech sends a reCAPTCHA v3 token (`g-recaptcha-response`) with every search. **WINDA (GWO) — manual.** Records are visible only to organisations with a WINDA profile, using the WINDA ID the technician gives them; winda.globalwindsafety.org is a login. **CISRS — manual.** cisrs.org.uk has no checker; CSCS Smart Check's website answers CAPTCHA_REQUIRED (its API is for approved IT partners only), and NOCN's Online Card Checker, the other CISRS checker, loads Google reCAPTCHA. **electrical_dk — manual for the person.** Erhvervsstyrelsen's autorisationsregister (sik.dk/registre/autorisationsregister; Sikkerhedsstyrelsen is part of Erhvervsstyrelsen since January 2026) is public with a CSV export (`/registries/export/csv/autorisationsregister`: 10,250 rows, 4,721 electrical; columns navn, adresse1–3, landkode, postnr, postdst, cvr, autnr, forretningsomr, binavn) and lists companies only — Denmark authorises the company, not the electrician, so it can confirm an employer's authorisation and never a candidate's. A register lookup calls no model; a verification's only model call is reading the uploaded document (`document-read`, item 16). **Step 2 (built 2026-09-15).** `cswip.ts` reads TWI's confirmed answer (`readCswip`): valid only when the answer carries the number asked about, expired → invalid, a candidate on the register with no certificate at the printed level → not_found, and any error → not_supported with an email to verification@twi.co.uk; the photograph and date of birth are never kept. `ampp.ts` is new (`readAmpp`): the number and surname go into the registry's filter with only their own characters; a listed credential is valid or invalid by its `expiration_date`; not listed, several holders under one surname, or no credential at the printed level → not_supported, never not_found; phone, address and city are never kept. `irata.ts` — the hosted browser past IRATA's reCAPTCHA — is deleted, and IRATA joins WINDA, CISRS and electrical_dk in `closed.ts`, which never fetch. **A register that could not answer said "not on the issuer register":** the lookup route passed `not_supported` to `stateFor` as null, so a register route turned BINDT being down, or CSWIP with no date of birth, into 'checked_not_found' (red) — a verdict nobody had reached; it now lands on 'pending_issuer' with the issuer email drafted. **0034 (`cert_routes_item23`, data only — applied by the owner on 2026-09-15; 6 cert_bodies and 11 cert_library rows confirmed):** AMPP issuer_email → register at nace.useclarus.com; CSWIP gains verification@twi.co.uk; IRATA register → issuer_email at techconnect; the instructions for WINDA, CISRS and electrical_dk restated; and the certificate library's built-in "How it is checked" lines (`cert_library.verification_route`, 11 English rows for CSWIP, AMPP, IRATA and CISRS, seeded from `tables.ts` by `scripts/seed-certs.ts`) — `tables.ts` was corrected too, but the screen reads the database copy, which showed "AMPP certification directory." and "IRATA — through the member company." beside the new answers. Before 0034 is applied the states are already right (the adapter's answer decides, whatever the stored route), but the cards show 0008's instructions — IRATA's still saying it is searched automatically — and the old library lines, and CSWIP's draft email has no address. **A certificate card pushed Verify 10px past a phone screen:** the "Checked where" link — AMPP's https://nace.useclarus.com/view/verify/ reached 397px and IRATA's https://techconnect.irata.org/verify/tech 400px on a 390px touch screen — could not wrap in the card's `1fr` column; design-shots' Verify shot has no certificate cards, so it never saw it. The column is `minmax(0, 1fr)` and the link breaks anywhere. (The probe's first diagnostic blamed the rail's links, which sit inside the nav's own scroll; it now lists only elements no ancestor clips.) `scripts/verify-adapters-check.ts` (in the gate) holds both parsers to fixture payloads in the confirmed shapes, the manual four to never fetching, every adapter but FROSIO's to no browser, and the state mapping. `scripts/verify-registers-probe.ts <base>` renders three SPECIMEN PDFs with made-up numbers, drops them through Verify as a signed-in recruiter at 1500px and 390px, and checks each card shows the document, the decoded title and the confirmation, lands on "pending issuer — email drafted" with its adapter's reason, and matches the rows; measured on those six certificates: 6 document-read attempts, €0.0340 — €0.0057 a verification — and no model call in any lookup. Since 0034 the probe also checks each card for the corrected instructions and "How it is checked" line, never the old wording (case-sensitive: IRATA's own note rightly says "not searched automatically"), and the drafted email's recipient — To verification@twi.co.uk, To customersupport@ampp.org, and no address for IRATA; on production on 2026-09-15, after the owner applied 0034, all 61 checks passed at 1500px and 390px. Not yet seen end to end: a real CSWIP or AMPP certificate that its register confirms — none is on file.
- **Item 24 — the candidate pool as a simple CRM (2026-09-15, done).** Owner's decisions, asked before building (queue, item 24): the ID shown is the plain number from the existing reference sequence — RFBT-P-0004 is #4 (`src/lib/candidate-number.ts`; 0035 stores it as the computed `candidate_number`), nothing renumbered, so public /v/<code> links and PDF names keep the code and new numbers have gaps where test runs used the sequence; stages New / Screening / Presented / Placed / Bench; every recruiter sees the whole pool, with a "Mine" filter (owner_id), no per-recruiter restriction. Found before building: 2 real candidates (both created by Verify), 7 certificates and 1 CV attached to nobody, `sends` (1 row) the only CV-sent record, no stage or placement anywhere, and Verify's CV intake matching on the name alone — an exact name silently rewrote that candidate's profile. **0035 (`candidates_crm`, applied by hand):** candidates.stage / stage_changed_at / stage_changed_by, employment_preference (permanent / contract / either), country (where they are based — not nationality), owner_id, data_retention_until, candidate_number; sends.client_name and note (the CV-sent log is the existing `sends`, not a second one); candidate_placements (client, placed_on, ended_on, placed_by, placed_at — "currently placed at AIBEL" is the open rows) with a workspace policy that has a `with check`. Until it is applied `hasCandidateCrm` keeps every screen as it was. **Step 1 — a CV dropped anywhere:** `CandidateDrop` (in the /app layout, for anyone whose Candidates screen is open; it stands aside on Verify, which keeps its own drop zone, and on onboarding) takes files dragged onto any page or chosen with the always-visible "Add CV" button (a phone cannot drag files) and sends them through Verify's intake unchanged. **Duplicates follow the WindEurope rule — a name plus a second field, never the name alone** (`src/lib/candidate-dedupe.ts#judgeDuplicate`, on top of name-match's exact/near): an exact or near name with an email, phone (last nine digits) or date of birth that agrees → asked as "likely the same person"; an exact name with nothing on either side to compare → asked as "same name only"; an exact name whose every comparable field differs → a new record, with a note that a namesake exists; otherwise a new record. Nothing is merged: the CV is stored and `AttachChoice` offers the match with its reason. This replaced Verify's own exact-name auto-update. Certificates and other non-CV documents still attach on an exact name in intake. The attach route refreshes a candidate's profile when a CV is attached to them (intake used to), issues a reference code through `nextReferenceCode` (it had the same null-code call), and sets owner_id when 0035 is there. `scripts/candidate-dedupe-check.ts` and `scripts/candidate-search-check.ts` are in the gate; `scripts/candidate-drop-probe.ts <base>` drops a real CV at 1500px and again at 390px and checks the second drop asks and attaches without a second record. On production a probe CV became #346 — the sequence had been used by test runs, as the ID decision said it would. **0035 applied by the owner 2026-09-15** (2 real candidates: numbers 4 and 9, owner backfilled from created_by). **Step 2 (04698c5) — one list, two views:** `src/lib/candidate-pool.ts#loadPool` reads the pool with the signed-in user's client (count first, then every 1,000-row page at once) and builds one folded search text per candidate — number, code, name, trade, based in, nationality, stage, preference, availability, notes, each certificate as a recruiter writes it ("cswip 3.1") and as the certificate words it ("level 3.1"), "sent to <client>" for every CV sent, "placed at <client>" and "currently placed at <client>" for an open placement — and `filterPool` applies the boolean query, Mine (owner, else creator), stage and preference. The table and the kanban (`CandidateBoard`: drag a card between New / Screening / Presented / Placed / Bench; every card and row also has `StageSelect`, which is how a touch screen moves someone) are the same rows; `?ref=<code>` (Today's links) is an exact search. `api/candidates/stage` writes with the user's client: Placed needs client and date and writes candidate_placements with placed_by and placed_at; leaving Placed asks for and writes the end date; every move records stage_changed_by and stage_changed_at. **The scale test found three bugs before anything shipped** (`scripts/candidate-pool-scale.ts`, 2,500 synthetic candidates in a test workspace): 0035's candidate_number read every digit in the code, so a code with digits before its number overflowed an integer — `0036_candidate_number_trailing.sql` takes the trailing number (applied by the owner 2026-09-15; the screens read the trailing number either way); the seeded ids were read in one query capped at 1,000 rows, so 1,523 CV-sent rows were written pointing at no candidate and blocked the probe account's deletion (removed by hand after checking the account was the probe's; `removeProbe` now deletes a probe user's CV-sent rows first, and the test reads ids in pages); and the certificate text read "cswip level 3.1 3.1", so "CSWIP 3.1" matched 0 of 126. **Measured at 2,500 candidates (3,690 certificates, 2,504 CV sent, 468 placements), signed in:** every query returns exactly what an independent filter over the seeded records returns, and a search over the loaded pool takes 0.6–1.6 ms; **the database read takes 4.4–5.2 s** (5.1–5.5 s before the pages were read in parallel; 3.6–3.7 s without certificate verifications) — the slow part, and a decision for the owner before the pool reaches that size. **RLS:** the gate's sweep reads candidates, sends, documents, verifications, scores and anonymized_cvs the same as a signed-in user and as the service role and finds no table with RLS on and no policy; candidate_placements has no rows, so counting cannot judge it — `scripts/candidate-rls-probe.ts` (two throwaway workspaces, in the gate from step 3) passed 8 of 8: A reads and writes their own candidate's CV-sent row and placements; B reads none of A's rows and cannot insert a placement into A's workspace or one in B's workspace pointing at A's candidate (0035's WITH CHECK), cannot change or delete A's placement, log a CV sent for A, or change A's stage. Smoke's "Candidates lists the candidate" check still finds the RFBT code, shown under each #N. `scripts/candidate-list-probe.ts <base>` passed on production at 1500px and 390px: table, search read back, a malformed search refused with where, Mine, a table stage move, a kanban drag to Placed recording AIBEL, "currently placed at aibel", and ending the placement from a phone. **Step 3 (34be025) — one candidate page, no tabs:** `candidates/[id]` reads with the signed-in client: details and edit (`api/candidates/[id]` PATCH; `src/lib/phone.ts` checks a phone for shape only — digits, a leading +, separators, 8 to 15 digits — and a refused field is named while the others save), employment preference, based-in country, keep-until date (data_retention_until), CVs sent (`api/candidates/[id]/sends`, client, sent_by, sent_at), placements with who entered them, the CV reading and the certificates in their three layers. `candidate-rls-probe` runs in every gate. The scale test also times candidates alone (0.29–0.35 s at 2,500) and option 1, the same data as four flat reads joined in memory (1.67–1.74 s, against 4.8 s embedded) — measured for the owner. **Owner's decision 2026-09-15: option 1.** `loadPool` now makes those four reads (each paged by id, all at once) and joins them in memory; a part that cannot be read fails the list rather than showing candidates without their certificates or placements. A stored search column with triggers (about 0.3 s) was turned down: a trigger that misses a case leaves the list silently stale, the bug class this codebase keeps meeting. Measured after the change at 2,500 candidates, signed in: 1912–1939 ms, every search still exactly the independent filter's answer. Revisit only if the real pool nears 2,500 and the wait is a real problem. **Step 4 (3f290a8) — certificates dropped on the page:** `CandidateDocDrop` posts to Verify intake with candidate_id, which attaches to that candidate whatever name the file carries (and says so when it differs), decodes and looks the certificate up as Verify does; Today's expiry lines open the page. **PostgREST returns at most 1,000 rows to one read, silently, and .limit() does not lift it** — intake compared a dropped CV with only a workspace's first 1,000 candidates, attach offered only 1,000, and test cleanup saw 1,000 of 2,500; `src/lib/all-rows.ts#allRows` reads every page, ordered by id. Read any table that can pass 1,000 rows through it. **Step 5 (7fa581f) — the CV and its original:** `candidates/[id]/cv` shows the stored reading in one standard view (contact, certifications, work history, languages and skills); `api/candidates/document` reads the document with the signed-in user's client and only then signs a 60-second storage link, named `candidate-<number>-<type>.<ext>` and never after the person — signed out it answers 401 and another workspace's document 404, with no link either way (`candidate-docs-probe --steps 5`). **Step 6 (7f2f422):** `candidates/[id]/documents/[docId]` shows a certificate's original (a PDF in a frame, or the image) beside its decoded reading, through the same route, and is a 404 unless the document is that candidate's. **Step 7 (f27cf95):** the Client version card runs `anonymize/enrich` for the candidate and says whether the PII check passed. **Step 8 — documents attached to nobody:** `scripts/link-candidate-documents.ts` (a report by default; `--write` links only what the app's own rules call unambiguous — a CV with one "likely" duplicate match, a certificate whose holder is an exact name match for one candidate). On 2026-09-15 the real workspace held 8: **0 linkable, 8 for manual review** — 7 certificates whose holders are not in the pool (Paul Daniel Pascale ×4, PEPLIŃSKI Szymon ×2, LICA IONEL) and 1 CV naming M. Marcu with no stored reading. `--write` was not run: there was nothing to link. **Closing walkthrough** (`scripts/candidate-walkthrough-probe.ts <base>`; owner's instruction: test workspaces, the sandblaster CV and test certificate fixtures, client "Test Client Ltd", every real step, nothing by URL or database): passed locally and on production on 2026-09-15 (19:18 UTC). At 1500px by mouse: sign in, drag the CV onto Home, Candidates from the rail, a typed search, open the row, drag the certificate onto the page, View original, edit phone, preference and country and Save, log a CV sent, a typed "sent to test client ltd", Kanban, drag the card to Placed with client and date, a typed "currently placed at test client ltd", open them, Read CV, Download original (the dropped file, byte for byte), Generate the client version (PII check passed). At 390px by touch: Add CV through the file picker, the certificate zone's file picker, a CV sent, the stage menu to Placed with client and date, Read CV, View original — no page scrolled sideways. 10 model attempts, €0.098, all metered as test traffic. The database is read only to confirm what each screen did. **A gate killed for memory keeps going.** On 2026-09-15 `next build` was killed by Windows three times (commit charge about 19 of 23 GB with Edge and a screensaver open), and the killed task's `release-gate.sh` went on to its next steps, which create throwaway accounts: stop the shell, let a running probe finish its own cleanup, and check for is_test workspaces before starting again. **Follow-up from the owner's review of production (2026-09-15) — a null date is never a date.** #9's McDermott entry read "01/01/1970 · logged by —". It was not a CV logged as sent: it was the pack "Send to this lead" (`api/leads/send-pack`, since 2026-09-09) attached on 2026-09-10, before item 24 — that route writes `sent_at: null` by design (prepared, not sent; nothing leaves except through /api/outreach) and wrote no `sent_by`, and item 24 read every `sends` row as a CV sent and passed the null to `new Date()`. It also made "sent to mcdermott" find #9. The CV-sent route itself always writes the signed-in user and a real date. `src/lib/cv-sent-entry.ts` now decides how a row reads: a real sent_at is a CV sent with its date; a null, unreadable or pre-2000 one is "pack prepared for a lead, not marked sent", "date not recorded", searched as "pack for <client>" and left out of the list's "CV sent to" column. send-pack records `sent_by` (who prepared it). `scripts/cv-sent-entry-check.ts` is in the gate. All `sends` on 2026-09-15: 1 row, that one — no other candidate had the epoch date.
- **Hiring contacts**: five sources, all evidence-gated through `appearsIn` — the advert, the organisation/leadership page, the company contact page, the Industry Contacts, then prepared searches. Measured on 17 real companies (2026-09-13): adverts 6, organisation page 3, switchboard 9, general email 5, Industry Contacts 0, and 6 companies where only searches remain. Only addresses on the company's own domain are kept. A pattern address is labelled `pattern` everywhere and `/api/outreach` refuses to send to one.
- **Industry Contacts are matched by company name, never by its first word.** `src/lib/attendee-match.ts#attendeesAt` is the only lookup (Hiring now's sheet, the lead draft's recipient, Radar's `attachPeople`): canonCompany equal, or — for a name of five characters or more — the full name followed by more words (`match: 'group'`, shown with the list's company). The old `ilike('%<first word>%')`, 25 rows, offered AF Gruppen three people from "Pan African University…" and "dg groep" two from "DG Competition", showed VESTAS 2 of its 99 people, and made 30 of Radar's 166 `lead_people` links to another company's people. Measured on the Copenhagen list (2026-09-14): Hiring now 7 companies offered (3 wrong, 8 right people) → 5 (13 right, none wrong); won-work companies 18 (12 wrong, 10 right) → 6 (13 right). `scripts/attendee-yield.ts [--relink]` re-measures and rewrites Radar's links. Relinked 2026-09-14: 138 links on 27 real leads, none to another company. That first run rebuilt each lead's ten from scratch and also swapped 40 right-company links for other people at the same company; --relink now keeps every link that still matches and only removes wrong ones. Industry Contacts live in `people`; `lead_people` only links them to leads. A second list joins through `scripts/import-attendees.ts` (0030: `seen_at_events`, `title_history`), one row per name + canonical company — a newer title replaces the old one, which is kept.
- **A matched Industry Contact is looked for by name on their company's own sites** (`src/lib/person-on-site.ts`, step 1c of the hiring-contacts job, `scripts/attendee-contacts.ts`). Only an address straight after the name, on the company's domain, whose local part fits that person, counts; a phone only between the two. NorSea's Klaus Iversen Grau was a correct match showing no address while norsea.dk printed his line and address on its WindEurope page. Free pages first (sitemap, home-page links — norsea.dk's sitemap lacked the page); a web search limited to the company's domains runs only from the script, under the daily cap. Stripped page text can run the number into the address ("41 70klaus.grau@…"): digits glued to an address after a number belong to the number.
- **Certificates**: `src/lib/certs/` decodes ISO 9606 designations and looks up 16 schemes — all in code, no model call. Three layers on the card and one plain-English line per certificate on the client PDF; the raw designation stays visible above the decoding and never reaches a buyer. An unrecognised certificate says so and raises one task per body+level. Seniors edit the library in Settings; a workspace row shadows the shipped one.
- **Sending**: `src/lib/send-capability.ts` is the single check — it asks Resend whether the domain is verified and caches like `schema-features`, so the button turns on by itself when it verifies. The button is never hidden, only disabled with the reason on it. Drafts generate regardless.
- **Campaigns**: batch + required documents; Today shows who is short and shouts inside three weeks. A document counts only when a file of that type is on the candidate.
- **Verify**: one drop zone, recognises by content. CV card streams Read → Anonymized → Bullets → PDF → Questions, match-aware when a job is selected. Adapters `frosio` (credential link) and `pcn` (BINDT register) live. The other six were re-checked live for item 23 — see the item 23 note: CSWIP and AMPP have public lookups, IRATA, WINDA, CISRS and electrical_dk stay manual.
- **Right to work**: `src/lib/right-to-work.ts` is the single rule, keyed on the **lead's** country. EU job → EU/EEA passport. UK job → an EU passport is *not* enough. `unknown` never passes. Search countries follow the job; never Serbia by default.
- **Screens**: Home (day < 8 lands here, logo always goes Home), Today, Leads (Won work / Hiring now), Verify, Pitch, Candidates, Campaigns, Settings, public `/v/[slug]`.
- **A row that opens a drawer says so without hover.** Leads and Hiring now rows use `src/components/OpenRow.tsx`: a click anywhere on the row opens it (links inside keep their own job), a chevron beside the company name is drawn at every width, and a "?" above each table says what the drawer holds. Smoke clicks a plain cell on desktop and taps at 390px on a touch screen.
- **Item 18 — industries, country filter, following.** Part 1: Won work has country chips built from every open won-work lead's country (not only the 50 per kind on screen), and `?country=` narrows both lists, their counts and every link out. Part 2: `src/lib/industry.ts` puts leads and companies in the 17 categories with no model call — an award by item 12's confirmed CPV codes only (the most specific entry covering a code decides; trade codes such as scaffolding and steelworks name no industry), a story by the words of its title, project name and first ten prose sentences, a Hiring now company by its advert titles and only the quoted words of its employer-type evidence (`companies.sector` is not used: it is partly model-assigned). Every tag keeps its words (`industry_evidence`). The first runs tagged stories from their site: a line a site prints on two or more of its stored stories (`repeatedSentences` — jacobs.com's data-center teaser, McDermott's "Operating in over 30 countries…"), a sentence naming three or more industries, a company's paragraph about itself, domain names in a menu ("DredgingToday.com"), and "energy transition" as background, now tag nothing. Measured 2026-09-14 before storing: Other / Uncategorized 65 of 172 — news 3/32, awards 45/117, Hiring now companies 17/23. `scripts/industry-report.ts --evidence` re-measures; `scripts/industry-backfill.ts --write` stores (0031); `src/lib/industry-store.ts` classifies as Radar, the TED ingest and the job crawl write, and never throws. Part 3: `users.industry_follow` (null: not chosen; `{all}`) and `users.industry_limit` (null: no cap — a placeholder for tiers, not tied to role). A new account is sent to `/app/onboarding` from `/app`, Home and the rail layout until it chooses (`mustChooseIndustries`, no query); existing accounts were set to all by 0032. Wind is one option covering Offshore and Onshore Wind; no other pair is grouped. The choice is saved only by `/api/me/industries` (the session's own row, the limit read from the database, never the request) and `/api/team/industries` (seniors, same workspace, within the member's own limit), through the service role — signed-in users still write nothing on users (0028) — and 0032's trigger refuses the same bad writes from anyone. `scripts/industry-follow-probe.ts` tries them with throwaway accounts in the release gate ("not judged" until 0032). Leads and Hiring now open on the followed industries with a banner and `?industries=all` one click away (the entitlement caps what is followed, never what can be seen); unclassified rows show; Today puts followed industries first. Preferences (`/app/preferences`) is for everyone; Settings shows seniors what the team follows. Probe accounts that test other screens follow all (`followAllForProbe`).
- **The crawls run on a tick, not a chain.** Radar, careers discovery and the job-post crawl used to pass each batch on over HTTP. Until 2026-09-14 the hand-off was a fetch abandoned after 1.5 s that never survived on Vercel: every scheduled morning from 11 to 14 September Radar read 5 of its 52 priority sources, and the job crawl and discovery did not run on their own after 10 September. That day's replacement waited for a 202 and was worse. Run by hand, every Radar batch started within two seconds of the first, the fifth was refused with **508 INFINITE_LOOP_DETECTED** (Vercel caps a function calling its own deployment, however it is timed — outbound fetches carry the caller's `x-vercel-id`), browserless answered 429, and the job crawl's batches, each selecting "least recently crawled" before the others had written, read the same companies: 13 of 347 boards. **`/api/jobs/tick`** (`src/lib/jobs/tick.ts`) now runs batches one after another inside one invocation, in process, starting none after 120 s: Radar from 04:00 UTC until today's run has read its last source (one source a batch — a batch of five took 268 s), careers discovery while a company is unchecked, the job crawl while a board is not crawled today (four a batch). **Supabase pg_cron calls it every five minutes (0029)** — a request the database starts is not recursion — and Vercel cron at 04:05 as a backstop. `job_ticks` records every tick and lets one run at a time; `scripts/schedule-report.ts [hours] --expect-cron` says whether the schedule is calling and what each tick did. `/api/jobs/radar`, `/job-posts` and `/careers-discovery` run one batch and hand nothing on; `src/lib/chain.ts#runInBackground` makes them answer 202, `?wait=1` returns the result. classify-employers, classify-sources, job-boards and resolve-domains still call their own deployment for the next batch, which Vercel refuses after a few hops: drive them one batch at a time from a script.
- **A check that turns on timing is worse than no check.** Three smoke checks have now had to be rewritten: one slept 1500 ms then asserted, one asserted an upstream outcome ("the upload made a candidate") on a screen that was behaving correctly, and one matched `Job Title|Positions|Responsibilities|Location` and passed on the word "Location" in the lead's own detail line whatever the drawer did. Wait for the thing, ask the database whether the assertion applies, and assert on a stable hook (`data-result`) rather than on wording a model chose. The fourth was lead-drawer-e2e's job-description proof, the same `Job Title|…|Location` match: on 2026-09-13 it failed with a 200 and a JD on screen that opened "Probe Offshore AS is seeking experienced Welders…". It now reads `data-result="jd"` too. A tap needs the page hydrated, not just rendered: a server-rendered row or "?" has no handler until React hydrates it, and on 2026-09-14 production smoke failed Leads at 390px twice that way — help shut, tap went nowhere — while the screen worked. `src/components/HydratedMark.tsx` sets `html[data-hydrated="true"]` under `/app`; wait for it before tapping.
- **A gate step must fail when what it checks is broken, and a cleanup must read its own deletes (2026-09-13/14).** design-shots printed its problems and exited 0; verify-e2e exited 0 unless sign-in failed; twelve scripts fired their user and workspace deletes unread, and verify-e2e's 20:06 UTC gate run left its user, workspace, a candidate, its CV and three documents behind — `candidates.created_by` and `documents.uploaded_by` block the user delete. `src/lib/test-data.ts#removeProbe` is the only cleanup (`clearContent` empties a test workspace in foreign-key order, one retry); a leftover fails the run. verify-e2e checks in the database that intake answered for all three files, the CV made a candidate, the certificate's lookup was recorded, the contract was stored and the client PDF passed the PII check. Verify's scripts wait on `data-verify-busy`, never on the words "Reading / Checking / Preparing / Writing" — a certificate card reads "Writing or approving procedures", and the wait never ended. A model reply with no JSON crashed `extractDocument` on `match(...)![0]` and marked a good CV unreadable, which `friendlyError` then blamed on the file type; `src/lib/ai/json-reply.ts` reads every reply (askJson too), and `scripts/json-reply-check.ts` feeds it the replies that broke.

**A job never calls its own deployment for its next batch.** A fire-and-forget `fetch` does not survive the response (the hiring-contacts route's next batch never ran), and one that waits is refused after a few hops with 508. Long jobs do one timed pass, record progress per row, and report what is left; the tick or a driver script calls again.

**`supabaseAdmin` sends `cache: 'no-store'`.** Next's Data Cache froze a query result across deploys and `/v/<slug>` served "Not found" for a candidate that existed.

## The queue
`LeadScout-prompt-queue.txt` is authoritative. These numbers are its numbers — never renumber them locally.

| # | Block | State |
|---|---|---|
| 1–3 | Domains → careers/ATS → job-post crawl → Hiring now | **done** |
| 4 | Job boards, secondary crawl | **done** — poster/employer kept separate, duplicates marked. Source list not yet narrowed to the named boards (Jobindex, Finn.no, EURES, Werk.nl, CV-Library, Reed) or pruned of aggregators. |
| 5 | Match-aware screening questions | **part done** — generated from the score, on the score card and in Verify. Still to do: the "Start screening call" view, answers saved against the candidate, answers feeding back into the score, and storing questions/answers with `lead_id` / `job_post_id` / `jd_version`. |
| 6 | Re-score + re-crawl | **part done** — DWT and Proserv contacts recovered; Nadara re-read as a Stage 1 false positive (permitting, not an award) and still needs a decision. Still to do: re-score every lead under the geography gate and fixed trade inference, and a "Re-check" action on the lead drawer. |
| 7 | Replies on the lead (Resend inbound) | **blocked** — waiting on the user's domain verification. |
| 8 | Campaigns + Today missing documents + expiry alerts | **part done** — campaigns, required documents, Today's missing-documents line, expired vs expiring. Still to do: received/verified/missing per document, "ready to send" count, "Send N packs", group numbers, 60/30/7 alerts with a drafted renewal, and an expired certificate marking a candidate unavailable for roles that require it. |
| 9 | New-hire path: trade cards, glossary, day gating | **done** — 10 trade cards and 88 glossary terms seeded, English only, rates and rotations deliberately unset |
| 10 | Compounding features — bench forecast, renewal radar, outcome learning | later |
| 11 | Talentmatch three — previously-worked-for, why this score, daily scorecard | **not started** |
| 12 | Tender-award extraction | **done, TED only** — Doffin waits for its API key; Udbud.dk dropped (everything it awards is on TED) |
| 13 | Hiring now — contacts, drawer, filters, Today, send path | **done** — contact discovery run over all 17 companies |
| 14 | Radar — stricter filtering, contact priority, email patterns | **done** — 0023 applied; verdicts, email patterns and source flags backfilled |
| 16 | Meter every model call | **done** — step 1 recruiter tools (f9d3a35), step 2 hiring-contacts real tokens + caps (8564e3e), step 3 Home's split; the first-week per-tool report is due 2026-09-22 (`scripts/cost-report.ts`) |
| 17 | Lead-age thresholds | **done** — 0024 and 0025 applied 2026-09-13 |
| 18 | Industry classification, country filter, recruiter follow, coverage report | **done** — all five parts live; 0031 backfilled, 0032 enforcement proven on production; watchlist 14 boards; 8 thin categories flagged for a scoped source item |
| 19 | Compound signal scoring | **done** — step 1 Won work fit (d10f3c1), step 2 Hiring now pressure and Today (47821ec); re-adverts are the posting signal, not a type (owner's decision) |
| 20 | Shared discovery pool | **design only — do not build** until the owner starts it as its own project; decisions and migration order are in the queue file |
| 22 | Spain coverage + "Industry Contacts" rename | **in progress** — rename done; Spain in resolve-domains, 38 approved first (owner); InfoJobs waits for API credentials the owner registers; Cádiz answered |
| 24 | Candidates pool — simple CRM | **done** — owner's decisions: ID #N from the code, five stages, full pool + Mine, list read option 1; 0035 and 0036 applied; drop anywhere (a2a64ef), list + kanban + boolean search (04698c5), candidate page (34be025), certificate drop (3f290a8), CV view + original (7fa581f), certificate original (7f2f422), client version (f27cf95), option 1 + walkthrough (18be13d); 8 loose documents, all for manual review |
| 23 | Six missing Verify adapters | **done** — step 1 findings (f280116); step 2 (1d2affd): CSWIP rewritten to TWI's confirmed answer and AMPP added (automatic), IRATA's browser past a reCAPTCHA retired, IRATA/WINDA/CISRS/electrical_dk manual; 0034 applied by the owner 2026-09-15 (6 cert_bodies, 11 cert_library rows confirmed) |
| 21 | Company-website contact discovery for Won work | **done** — engine and cron secret (46f7da5), Won work display and the sign-in fix (7844f38), measured run: 10 of 149 leads gained a contact, paid search €0.42 for nothing. The 103 companies with no website get prepared searches only; paying to find their sites is the owner's call on this yield |
| — | Design pass (Claude Design MCP) | after real data is on screen |

Also outstanding, outside the queue: watching the first unattended overnight run of the recheck cron and the first Radar morning driven by pg_cron (`scripts/schedule-report.ts`).

Commit to main, two lines per step. Keep files small; one screen per file; no UI libraries. Design tokens live in `tailwind.config.js`.
